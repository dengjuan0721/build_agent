
# planner_graph
INITIAL_PLAN_PROMPT = """你是一位资深内容策略专家。你的任务是围绕给定主题，为一篇文章创建一个宏观的、初步的大纲。

请注意，这仅是计划的初稿，重点在于快速构建框架。

主题：{task}

请提供一个结构清晰的大纲，包含主要章节以及各章节下的核心要点（以项目符号形式呈现）。"""
CRITIQUE_PLAN_PROMPT = """你是一位批判性思维专家。你的任务是深入分析下方的内容大纲，并找出其潜在的弱点。

请提供一份一针见血且富有建设性的评审意见。请重点考量以下几个方面：
- 结构是否逻辑清晰？
- 主题范围是过宽还是过窄？
- 是否遗漏了任何关键要点？
- 大纲的详细程度是否足以让写作者毫无歧义地执行？

待评审的内容大纲：
---
{plan}
---
"""

REFLECTION_AND_REFINEMENT_PROMPT = """你是一位首席规划师，正在对一份内容大纲进行最终定稿。你的任务是综合下方的【原始大纲】和【评审意见】，产出一份**最终版（FINAL）**的、可直接交付给写作者执行的全新大纲。

**原始大纲：**
---
{plan}
---

**评审意见：**
---
{critique}
---

你的目标是生成一份**修订后且最终确定**的计划。这份最终大纲必须满足以下所有要求：
1.  **极度详尽**: 包含所有必要的细节，确保写作者不会产生疑问。
2.  **清晰明确**: 逻辑结构清晰，标题和要点准确无误。
3.  **可执行性强**: 每个部分都具体到可以立即动笔的程度。
4.  **完全整合**: 必须完全吸收【评审意见】中的所有反馈。
5.  **语言要求**: 最终大纲必须使用**中文**撰写。

现在，请生成**最终的、完整的、经过优化的内容大纲**。"""

RESEARCH_PLAN_PROMPT = """你是一位研究员，负责为接下来的文章写作提供信息支持。

请针对以下文章主题，生成一个搜索查询列表，用于收集所有相关信息。

主题：{task}

重要：最多只生成 3 个搜索查询。"""

RESEARCH_CRITIQUE_PROMPT = """你是一位研究员，你的任务是根据下方的修订要求，为文章的修改工作提供必要的信息支持。

请生成一个搜索查询列表，以收集任何与修订相关的补充信息。

重要：最多只生成 3 个搜索查询。"""

#parser prompt
INITIAL_PARSE_PROMPT = """你是一位高度智能的文档结构分析专家。你的任务是将一份半结构化的 Markdown 文章大纲，解析成一个严格遵循所提供 Pydantic 递归模型的、精确的、层次化的 JSON 对象。

**你的核心目标是理解并完美呈现其【层级结构】。**

-   **识别顶层章节**: 找出所有最高级别的章节标题 (例如：“引言”、“第一章：前 Transformer 时代”、“结论”)。
-   **识别子要点/子章节**: 在每个章节内部，识别其下的子要点或子章节 (例如：“* **引子：**”、“* **1.1 序列处理的瓶颈：**”)。
-   **处理嵌套结构**: 如果一个子章节自身还包含更深层次的项目符号列表，你必须将其表示为一个嵌套的 `sub_sections` 列表。
-   **分配描述文本**: 'details' 字段应包含与标题相关联的解释性文本。

**关键指令**: 最终的 JSON 必须是 Markdown 结构的一个完美的、无损的嵌套表示。**严禁将层级结构扁平化**。

**待解析的 Markdown 大纲：**
---
{unstructured_plan}
---
"""

REFLECTION_PARSE_PROMPT = """你是一位质量保障专员（QA）。你的任务是审查一个从 Markdown 大纲解析出的结构化 JSON 对象。你必须将该 JSON 与原始 Markdown 进行对比，以找出任何差异、信息遗漏或结构误解。

同时，你还需要从**语义正确性**和**对下游写作智能体的可用性**角度，验证这个 JSON 对象的质量。

**1. 原始 Markdown 大纲：**
---
{unstructured_plan}
---

**2. 待验证的已解析 JSON：**
---
{parsed_json}
---

**你的任务：**
请严格对照原始 Markdown，对已解析的 JSON 进行批判性审查。请重点关注以下质量标准，并提供一份评审报告：

**检查点 1：标题的适用性 (最重要!)**
-   'title' 字段的内容是真正面向读者的标题，还是仅仅是计划中的指导性标签？
-   像 **'应用与用例'、'局限与挑战'、'影响与遗产'、'结论'** 这样的指导性标签，因其过于宽泛，**不能作为有效的子章节标题**。
-   请将这些宽泛的标题替换为更具体、更具描述性和批判性的措辞。
-   **行动指令**: 如果发现指导性标签被用作标题，你的评审报告**必须**建议将它们合并到其父章节的 'details' 字段中，而不是作为独立的子章节处理。

**检查点 2：结构完整性与无损性**
-   JSON 的层级结构是否准确反映了 Markdown 中的嵌套关系？
-   原始 Markdown 中的任何文本是否在 JSON 中丢失了？

**检查点 3：细节分配的合理性**
-   'details' 字段中的内容是否与其对应的 'title' 恰当匹配？

你的输出必须是一份概述所有必要修改的简洁评审报告。如果 JSON 完美无缺并通过了所有检查，请只回复确切的短语 **"PERFECTLY_PARSED"**。
"""

REVISION_PARSE_PROMPT = """你是一位专门处理数据修正的专家级数据结构化智能体。你收到了一份关于你上次解析 Markdown 尝试的评审报告。

你的任务是生成一个新的、修正后的 JSON 对象，该对象必须完全解决评审报告中提出的所有问题。

**1. 原始 Markdown 大纲 (供参考):**
---
{unstructured_plan}
---

**2. 上一个（有缺陷的）已解析 JSON:**
---
{parsed_json}
---

**3. 你必须解决的评审报告：**
---
{critique}
---

**你的任务：**
生成一个**全新的、完整的、修正后**的 JSON 对象，修复评审报告中提到的所有问题。
请确保新的 JSON 是对原始 Markdown 大纲的完美、无损的表示，并且完全符合 Pydantic 模型的要求。
"""

# connect

CONNECTOR_WRITER_PROMPT = """你是一位文笔大师兼专家级编辑，尤其擅长打造流畅自然的叙事衔接。你的任务是为文章的一个主要章节撰写 **{mode}** 段落（mode 指“引言”或“总结”）。

**全局背景 (总体规划):**
(这部分定义了整篇文章的目标和基调)
---
文章目标: {objective}
文章语调: {tone}
目录 (你的当前位置已用 '>>>' 标出):
{table_of_contents}
---

**当前章节规划:**
(这是你需要为其撰写引言/总结的章节之规划)
---
章节标题: {section_title}
章节目标: {section_goal}
---

**待衔接的子章节内容:**
(这是由其他智能体撰写的、需要你进行衔接与总结的详细内容)
---
{sub_sections_content}
---

**你的核心使命:**
撰写一个流畅且引人入胜的段落，为本章节进行 **{mode}**。

-   当撰写**引言 (intro)** 时：请解释**为什么**本章节至关重要，并自然地引出下方的子章节。
-   当撰写**总结 (conclude)** 时：请对上文的子章节内容进行简要的、画龙点睛式的回顾与收束。

**关键要求**:
该段落必须与目录中可见的**前一个**主要章节形成逻辑上的无缝衔接。请使用**中文**写作。
"""

CONNECTOR_REFLECTION_PROMPT = """你是一位正在审阅过渡段落的批判性编辑。

**背景信息:**
该段落旨在为标题为“{section_title}”的章节进行 **{mode}**。
它的作用是衔接前文内容与下方列出的子章节，并必须与文章的总体目标“{objective}”保持一致。

**其所衔接的子章节内容:**
---
{sub_sections_content}
---

**待审阅的草稿:**
---
{draft}
---

**你的任务:**
请严格评审此草稿。它是否引人入胜？是否创造了流畅自然的过渡？语调是否准确？

-   如果草稿完美无缺，请只回复确切的短语 **"PERFECTLY_WRITTEN"**。
-   否则，请提出具体的、可操作的改进要点。
"""

CONNECTOR_REVISION_PROMPT = """你是一位文笔大师兼专家级编辑。你之前为一段 **{mode}** 段落撰写了草稿，但收到了一些评审意见。
你的任务是**重写这份草稿**，并完全融入下方提供的所有反馈。

**原始背景信息 (供参考):**
---
- 文章目标: {objective}
- 文章语调: {tone}
- 章节标题: {section_title}
- 待衔接的子章节内容:
{sub_sections_content}
---

**你之前的（有缺陷的）草稿:**
---
{draft}
---

**你必须解决的评审意见:**
---
{critique}
---

**你的核心使命:**
生成一个**全新的、经过改进的**段落版本，修复评审意见中提到的所有问题。新版本必须是一个独立、流畅、浑然天成的段落。
"""

# writer

SECTION_PLAN_PROMPT = """你是一位严谨且富有创造力的微观规划师。你的任务是为一个大篇文章中的**某一个具体子章节**，创建一个详细的“行动计划”。

**全局背景与你的定位:**
-   **文章总目标:** {objective}
-   **文章总基调:** {tone}
-   **你在文章中的位置 (已用 '>>>' 标出):**
    ```
    {table_of_contents}
    ```

**分配给你的子章节 (来自总体规划):**
(这是你需要为其制定行动计划的具体部分)
---
{section_to_write_json}
---

**你的核心使命:**
基于以上所有信息，创建一个“微观执行计划”。该计划是一系列给写作者的可执行指令。具体来说，请规划并列出以下内容：
1.  **核心论点 (Key Talking Points):** 将主要指令分解为 3-5 个具体的、有序的、必须阐述清楚的要点。
2.  **阐释策略 (Explanatory Strategy):** 针对每个要点，决定最佳的解释方式 (例如：使用类比、举出现实世界的例子、进行分步拆解)。
3.  **必需元素 (Required Elements):** 指明本节是否需要特殊元素，如 `[代码示例]`、`[图表]` 或 `[引用来源]`。
4.  **衔接建议 (Transitions):** 建议一个能吸引读者的开场句，以及一个能自然过渡到下一主题（可在目录中看到）的收尾句。
"""

SECTION_RESEARCH_PLAN_PROMPT = """你是一位高效的研究助理。你的任务是为执行一份“微观执行计划”生成有针对性的搜索查询。

**你的工作背景:**
-   **文章总目标:** {objective}
-   **需要进行研究的微观计划:**
    (这是你需要为其搜集信息的详细行动计划)
    ---
    {micro_plan}
    ---

**你的核心使命:**
请仔细阅读这份微观计划。找出其中需要外部信息（例如：代码、特定定义、事实数据）的要点。
为 Tavily 搜索引擎生成 2-3 个高度具体的搜索查询，以找到这些信息。
"""

SECTION_WRITER_PROMPT = """你是一位专家级的中文技术作家。你的任务是根据一份精确的行动计划，为**一个特定的子章节**撰写完整、详尽的内容。

**全局背景与你的定位:**
-   **文章总目标:** {objective}
-   **文章总基调:** {tone}
-   **你在文章中的位置 (已用 '>>>' 标出):**
    ```
    {table_of_contents}
    ```

**你的行动计划 (微观执行计划):**
(这是你的首要蓝图。你**必须严格执行**此计划中的每一个要点。)
---
{micro_plan}
---

**辅助研究材料:**
---
{content}
---

**你的核心使命:**
为该子章节撰写完整且最终的 Markdown 格式正文。输出必须是**纯粹的正文内容**（一个或多个段落）。

**关键要求**:
-   **请勿**自创任何子标题，完全遵循微观计划的结构。
-   直接开始输出正文，**不要**在开头添加本小节的标题作为 Markdown 标题。
-   请用**中文**写作，并注意表达形式的多样化，例如在必要时使用表格、图示或直观的代码示例。仅在少量代码能达到极佳解释效果时，才考虑添加代码。
"""

SECTION_REFLECTION_PROMPT = """你是一位严谨且要求苛刻的编辑。你必须严格对照一份行动计划，来批判性地审查一份子章节的草稿。

**全局背景:**
-   **文章总目标:** {objective}
-   **文章总基调:** {tone}

**行动计划 (你的唯一评判标准):**
(这是写作者本应遵循的指令集)
---
{micro_plan}
---

**待审阅的草稿:**
---
{draft}
---

**你的核心使命:**
提供一份一针见血且富有建设性的评审报告。
-   写作者是否遵循了“行动计划”中的**每一条**指令？
-   文章是否清晰、引人入胜，且与整体基调和目标保持一致？

如果草稿完美无缺，请只回复确切的短语 **"PERFECTLY_WRITTEN"**。
"""

SECTION_RESEARCH_PROMPT = """你是一位研究助理。请根据下方的评审意见，生成 2-3 个有针对性的搜索查询，以帮助写作者解决反馈中提到的问题。

**需要解决的评审意见:**
---
{critique}
---
"""

PROPOSE_EDIT_PROMPT = """你的任务是根据用户提供的指令，精确地修订给定的原始段落。
请仔细阅读用户提供的所有信息，然后生成一个修订后的版本，并附上你做出修改的简要理由。

<原始段落>
{original_content}
</原始段落>

<用户指令>
{initial_user_prompt}
</用户指令>

如果这是基于先前反馈的修订，以下是反馈内容：
<先前反馈>
{revision_history_str}
</先前反馈>
"""

SUMMARIZE_EDIT_HISTORY_PROMPT = """你是一个专业的编辑助理。请将以下完整的编辑修订过程总结成一段简洁、清晰的日志。
日志应该以客观的口吻，说明用户的初始目标是什么，中间经历了怎样的关键修改步骤，以及最终达成的修改结果。

<初始指令>
{initial_user_prompt}
</初始指令>

<最终采纳的段落>
{final_proposal_content}
</最终采-纳的段落>

<详细修订历史>
{revision_history_str}
</详细修订历史>

请根据以上信息，生成一段不超过150字的摘要总结。
"""

SUMMARIZE_USER_INTENT_PROMPT = """你是一位精通指令理解的AI助理。你的任务是将用户原始的、可能口语化的编辑请求，总结成一个清晰、可执行的、分点的修改意图列表。

请只关注核心的、可操作的指令，忽略客套话或无关的评论。

例如，如果用户说：“你好，我觉得这段话有点啰嗦，而且口吻不太对，感觉像在自言自语，能不能帮我改得更面向读者一些？谢谢！”，你应该总结为：
- 缩短段落，使其更简洁。
- 调整叙事口吻，使其直接面向读者。

现在，请总结以下用户的编辑请求：
---
{raw_user_prompt}
---
"""